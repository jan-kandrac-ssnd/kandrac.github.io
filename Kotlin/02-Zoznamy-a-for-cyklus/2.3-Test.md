# 2.3 Test - Zoznamy a for cyklus (2.1 - 2.2)

Over si svoje vedomosti z kapitol o zoznamoch a cykle for. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 14</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question"><code>List&lt;String&gt;</code> je z hľadiska použitia v Kotline podobný <code>vector</code>-u z jazyka C++.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>List</code> sa z jazyka C++ najviac podobá na <code>vector</code> alebo klasické pole.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Do nemeniteľného <code>List</code>-u môžeme kedykoľvek priradiť novú hodnotu na existujúci index.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>List</code> nemá rád zmeny - priradenie novej hodnoty na existujúci index (napr. <code>zoznam1[0] = "Marek"</code>) spôsobí chybu kompilácie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>MutableList</code> umožňuje meniť hodnoty na existujúcich indexoch.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>zoznam4[0] = "Marek"</code> je pri <code>MutableList</code> v poriadku.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Konštruktor <code>List(10) { i -&gt; i }</code> vytvorí zoznam s 10 prvkami, kde <code>i</code>-tý prvok má hodnotu <code>i</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Konštruktor prijíma veľkosť poľa a funkciu, ktorá určí hodnotu pre <code>i</code>-tu položku.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Operátor <code>+=</code> dokáže pridať nový prvok do <code>MutableList</code>-u.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>zoznam4 += "Lukas"</code> pridá nový prvok na koniec zoznamu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Zápis <code>0..5</code> v cykle <code>for</code> pri iterácii obsahuje aj hodnotu <code>5</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>for (i in 0..5) print(i)</code> vypíše <code>012345</code>, teda vrátane <code>5</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Zápis <code>0..&lt;5</code> v cykle <code>for</code> pri iterácii obsahuje aj hodnotu <code>5</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>for (i in 0..&lt;5) print(i)</code> vypíše iba <code>01234</code> - horná hranica je vylúčená.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Cyklus <code>for</code> v Kotline nedokáže iterovať priamo cez zoznam (<code>List</code>), iba cez rozsahy (<code>Range</code>).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Iterátor môže nadobúdať hodnoty priamo zo zoznamu, napr. <code>for (i in listOf("Jan", "Fero", "Mark"))</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>downTo</code> sa používa napríklad na zostupnú iteráciu v cykle <code>for</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Spolu so <code>step</code> patrí medzi menej časté, ale užitočné zápisy cyklu <code>for</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>step</code> určuje krok, o ktorý sa iterátor cyklu posúva.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>step</code> mení, o koľko sa hodnota iterátora zvyšuje (alebo znižuje) medzi jednotlivými iteráciami.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Zápis <code>MutableList(10) { i -&gt; i }</code> s konštruktorom je v praxi zápis, ktorý používame veľmi často.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Zápis s konštruktorom takmer nepoužívame - slúži skôr na vysvetlenie, ako zoznamy fungujú.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Kotlin neponúka žiadnu funkciu na zamiešanie (náhodné premiešanie poradia) prvkov zoznamu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Takáto funkcia existuje (napr. <code>shuffled()</code>) - jednou z úloh v lekcii je práve ju nájsť a použiť.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>zoznam4.add(1, "Tom")</code> vloží prvok na index <code>1</code> a ostatné prvky posunie o pozíciu ďalej.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Funkcia <code>add(index, hodnota)</code> vloží prvok na daný index a posunie zvyšné prvky zoznamu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Zoznam typu <code>Range</code> (napr. <code>0..5</code>) je istou formou <code>List</code>-u.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Zápis <code>0..5</code> vytvára premennú typu <code>Range</code>, ktorá je istou formou <code>List</code>-u.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 14</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
