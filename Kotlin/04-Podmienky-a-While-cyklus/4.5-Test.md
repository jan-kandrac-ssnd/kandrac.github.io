# 4.5 Test - Podmienky a while cyklus (4.1 - 4.4)

Over si svoje vedomosti z kapitol o podmienkach <code>if</code>, <code>when</code> a cykle <code>while</code>. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 15</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Blok kódu za <code>if</code>-om sa vykoná len vtedy, keď výraz v zátvorke vráti hodnotu <code>true</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Podmienka nemusí byť len porovnávanie - stačí čokoľvek, čo vracia <code>true</code>/<code>false</code>, napr. <code>"hello".contains("hell")</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Podmienka v <code>if</code>-e môže obsahovať iba porovnávacie operátory (napr. <code>&lt;</code>, <code>&gt;</code>, <code>==</code>).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Podmienkou môže byť čokoľvek, čo vráti <code>Boolean</code>, napríklad volanie funkcie ako <code>"hello".contains("hell")</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Pri reťazci <code>if - else if - else</code> sa vykoná vždy len jedna vetva - tá, ktorej podmienka je splnená ako prvá.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ostatné podmienky sa už ani neoverujú, ak je jedna z predchádzajúcich splnená.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak nie je splnená žiadna podmienka v reťazci <code>if - else if</code> a chýba <code>else</code>, nevykoná sa žiadny blok.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez záverečného <code>else</code> sa program jednoducho posunie ďalej, ak nebola splnená žiadna z podmienok.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>when</code> v Kotline je obdobou <code>switch-case</code> z iných jazykov.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>when</code> nahrádza <code>switch-case</code>, no vykoná sa vždy len tá vetva, ktorá je splnená ako prvá.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>when</code> s parametrom nemôže mať vetvu <code>else</code> pre nezhodné hodnoty.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>else</code> vetva sa vykoná práve vtedy, keď nie je splnená žiadna z predchádzajúcich podmienok.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>when</code> môže byť použité aj bez parametra, kedy sa každá vetva správa ako samostatná podmienka.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Hodí sa to najmä vtedy, keď sa podmienky týkajú viacerých premenných naraz.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak <code>if</code> alebo <code>when</code> vymenujú všetky možnosti (napr. pomocou <code>else</code>), hovoríme, že sú úplné (angl. <em>exhaustive</em>).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Úplná podmienka sa vďaka tomu stáva výrazom (expression), pretože sa vždy vykoná presne jedna vetva.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Úplný <code>if</code> alebo <code>when</code> výraz vráti hodnotu podľa posledného výrazu vo vykonanej vetve.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Toto platí aj pre bloky s viacerými príkazmi - vráti sa vždy hodnota posledného výrazu vo vetve.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Statement (napr. <code>x = 5</code>) je kúsok kódu, ktorý nevracia žiadnu hodnotu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Statement je kúsok kódu, ktorý hodnotu nevracia, na rozdiel od výrazu (expression).</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Expression (výraz, napr. <code>x &gt; y</code>) je kúsok kódu, ktorý vždy vracia nejakú hodnotu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Výraz je taký kus kódu, ktorý vracia nejakú hodnotu, napríklad výsledok porovnania.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Cyklus <code>while</code> vždy vykoná svoje telo aspoň raz, aj keď podmienka nie je splnená.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Podmienka sa overuje ako prvá, takže telo cyklu <code>while</code> sa nemusí vykonať ani raz.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Vo cykle <code>while</code> sa podmienka vyhodnocuje pred každým vykonaním tela cyklu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Cyklus opakuje telo, kým je podmienka splnená - vždy sa najprv overí podmienka.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>return</code> vo funkcii okamžite preruší jej vykonávanie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad vo funkcii <code>main</code> môžeme použiť <code>return</code> na okamžité ukončenie po vypísaní správy.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Cyklus <code>while</code> sa nedá použiť na opakované čítanie zo štandardného vstupu, kým používateľ nezadá správnu hodnotu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Práve naopak, toto je jedno z typických použití cyklu <code>while</code> - opakovane čítať vstup, kým nie je splnená požadovaná podmienka (napr. správne heslo).</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 15</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
