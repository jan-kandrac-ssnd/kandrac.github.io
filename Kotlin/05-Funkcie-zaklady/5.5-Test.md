# 5.5 Test - Funkcie - základy (5.1 - 5.4)

Over si svoje vedomosti z kapitol o funkciách. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 12</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Funkciu definujeme kľúčovým slovom <code>fun</code>, za ktorým nasleduje jej názov.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>fun compute(a: Int, b: Int): Int { ... }</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Argumenty funkcie sú v Kotline predvolene <code>var</code>, preto sa dajú v tele funkcie meniť.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Argumenty funkcie sú vždy <code>val</code> - preto pri nich slovíčko <code>val</code> ani nepíšeme.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia bez explicitne uvedeného návratového typu vracia typ <code>Unit</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Zápis <code>fun x() {}</code> je skrátený zápis pre <code>fun x(): Unit {}</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Hodnotu z funkcie vraciame kľúčovým slovom <code>return</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Za <code>return</code> nasleduje hodnota, ktorú chceme z funkcie vrátiť.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak sa telo funkcie skladá z jediného výrazu, nie je potrebné písať <code>return</code> ani zložené zátvorky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>fun compute(a: Int, b: Int) = a * b</code> je platný jednoriadkový zápis.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Pri jednoriadkovom zápise funkcie (<code>fun x() = ...</code>) musíme vždy explicitne uviesť jej návratový typ.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Návratový typ je zjavný z výrazu, takže ho netreba písať - hoci pri zložitejších funkciách je to dobrým zvykom.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Argumenty s predvolenými hodnotami obvykle umiestňujeme na koniec zoznamu parametrov funkcie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Hoci to nie je nevyhnutné, obľubujeme dávať parametre s predvolenou hodnotou na koniec.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Ak má parameter predvolenú hodnotu, pri volaní funkcie ho musíme vždy explicitne vyplniť.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Práve naopak - ak ho nevyplníme, použije sa jeho predvolená hodnota.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Named arguments (pomenované argumenty) umožňujú vymenovať iba tie parametre, ktoré chceme nastaviť.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ostatné parametre zostanú nedotknuté (použijú svoju predvolenú hodnotu).</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Pri použití named arguments musí byť poradie parametrov vo volaní rovnaké ako v definícii funkcie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Práve výhodou named arguments je, že poradie parametrov vo volaní môže byť ľubovoľné.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Named arguments sa oplatí použiť aj vtedy, keď z volania funkcie nie je jasné, čo daný argument reprezentuje (napr. <code>substring(3, 5)</code>).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Zápis <code>"Hello World".substring(startIndex = 3, endIndex = 6)</code> je oveľa čitateľnejší.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak chceme nastaviť len jeden z posledných predvolených parametrov bez named arguments, musíme vymenovať aj všetky predvolené parametre pred ním.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Práve preto sa v takom prípade radšej používajú named arguments namiesto vymenovania všetkých predchádzajúcich parametrov.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 12</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
