# 6.4 Test - Triedy a Objekty (6.1 - 6.3)

Over si svoje vedomosti z kapitol o triedach, objektoch a konštruktoroch. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 15</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Trieda (class) je šablóna, zatiaľ čo objekt je jej konkrétna inštancia.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Trieda definuje atribúty a metódy, objekt je konkrétny záznam vytvorený podľa tejto šablóny.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Atribút je premenná patriaca triede, metóda je funkcia patriaca triede.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>meno</code> je atribút, <code>zdravSa()</code> je metóda.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Encapsulácia znamená, že všetky atribúty triedy majú byť verejné (<code>public</code>), aby k nim mal prístup ktokoľvek.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Encapsulácia je presný opak - interné údaje skryjeme (napr. <code>private</code>) a poskytneme len verejné rozhranie cez metódy.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Trieda sa v Kotline definuje kľúčovým slovom <code>class</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>class Person { ... }</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">V Kotline sú všetky členy triedy implicitne <code>private</code>, kým ich neoznačíme ako <code>public</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Je to naopak - v Kotline je všetko implicitne <code>public</code>, takže žiadne modifikátory prístupu netreba písať.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Primárny konštruktor sa dá zapísať priamo v hlavičke triedy, napríklad <code>class Person(val name: String)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Je to skrátený zápis pre triedu s atribútmi a konštruktorom.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Atribút deklarovaný v primárnom konštruktore pomocou <code>var</code> sa dá po vytvorení objektu meniť.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>counter.count = 5</code> je v poriadku, ak je <code>count</code> deklarovaný ako <code>var</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Blok <code>init</code> sa vykoná až pri prvom volaní metódy na objekte, nie pri jeho vytvorení.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Blok <code>init</code> sa vykoná ihneď pri vytvorení (inicializácii) objektu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>require()</code> vyhodí <code>IllegalArgumentException</code>, ak je jej podmienka vyhodnotená ako <code>false</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Často sa používa v bloku <code>init</code> na kontrolu vstupných hodnôt, napr. <code>require(age &gt;= 0) { "Vek nemoze byt zaporny" }</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Sekundárny konštruktor musí vždy zavolať primárny konštruktor (alebo iný sekundárny) pomocou <code>this(...)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak trieda má primárny konštruktor, každý sekundárny ho musí zavolať cez <code>this(...)</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak trieda nemá primárny konštruktor, sekundárne konštruktory nemusia volať <code>this(...)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> V takom prípade je ale lepšie použiť blok <code>init</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Trieda môže mať naraz viacero sekundárnych konštruktorov s rôznymi parametrami.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad trieda <code>Config</code> z lekcie má dva rôzne sekundárne konštruktory.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Dedičnosť umožňuje znovupoužiť kód rodičovskej triedy v podtriede.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Podtrieda môže pridať vlastné atribúty a metódy, no zdedený kód nemusí písať nanovo.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">V Kotline musíme na vytvorenie inštancie triedy vždy zavolať jej konštruktor pomocou zátvoriek, napr. <code>Person()</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Objekt vytvoríme volaním konštruktora, napr. <code>val person = Person()</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Trieda <code>Counter(var count: Int)</code> umožňuje zmeniť hodnotu atribútu <code>count</code> po vytvorení objektu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Keďže <code>count</code> je deklarovaný ako <code>var</code>, jeho hodnotu je možné neskôr meniť.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 15</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
