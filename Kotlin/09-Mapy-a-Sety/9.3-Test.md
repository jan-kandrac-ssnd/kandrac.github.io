# 9.3 Test - Mapy a Sety (9.1 - 9.2)

Over si svoje vedomosti z kapitol o mapách a setoch. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 14</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Mapa (<code>Map</code>) ukladá dvojice kľúč - hodnota, pričom každý kľúč je v mape unikátny.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak by sme priradili hodnotu k existujúcemu kľúču, pôvodná hodnota by sa jednoducho prepísala.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Prístup k neexistujúcemu kľúču v mape pomocou <code>map["neco"]</code> spôsobí pád programu s výnimkou.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Vráti <code>null</code>, žiadna výnimka sa nevyhodí - preto je typ hodnoty vlastne nullovateľný (<code>Int?</code> a pod.).</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>getOrDefault(kľúč, predvolenáHodnota)</code> vráti predvolenú hodnotu, ak kľúč v mape neexistuje.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>map1.getOrDefault("Marek", 0)</code> vráti <code>0</code>, ak kľúč <code>"Marek"</code> v mape neexistuje.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Zápis <code>"Janko" to 25</code> je skrátený spôsob vytvorenia dvojice (<code>Pair</code>).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Infixová funkcia <code>to</code> vytvorí objekt typu <code>Pair&lt;String, Int&gt;</code>, ktorý sa použije napr. vo funkcii <code>mapOf</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>mapOf(...)</code> vytvorí meniteľnú mapu, do ktorej môžeme priamo pridávať nové páry.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>mapOf</code> vytvorí needitovateľnú (immutable) mapu. Na pridávanie a mazanie potrebujeme <code>mutableMapOf</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Do <code>MutableMap</code> vieme pridať alebo zmeniť hodnotu zápisom <code>map["kľúč"] = hodnota</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak kľúč ešte neexistuje, pridá sa nový pár. Ak už existuje, jeho hodnota sa prepíše.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Iteráciou <code>for ((name, age) in map)</code> vieme deštruovať dvojicu kľúč-hodnota priamo v hlavičke cyklu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Kotlin pri iterácii nad mapou umožňuje priamu deštrukciu na premenné pre kľúč a hodnotu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Vlastnosť <code>map.keys</code> vráti množinu (<code>Set</code>) všetkých kľúčov v mape.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Podobne <code>map.values</code> vráti kolekciu všetkých hodnôt v mape.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Set (<code>Set</code>) obsahuje každú hodnotu nanajvýš raz - duplicity sa automaticky odstránia.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>setOf(1, 2, 3, 3, 2)</code> vytvorí set <code>{1, 2, 3}</code> - duplicitné hodnoty zmiznú.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Set, na rozdiel od Listu, má striktne definované poradie prvkov, ktoré sa dá indexovať pomocou <code>[]</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Set nemá definované poradie prvkov a nepodporuje indexovanie ako <code>List</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Operátor <code>in</code>, napr. <code>3 in set3</code>, overí, či sa daná hodnota v sete nachádza.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Vráti <code>true</code>, ak sa hodnota v sete nachádza, inak <code>false</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>intersect</code> vráti prienik dvoch setov - prvky, ktoré sa nachádzajú v oboch.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>setA.intersect(setB)</code> pre <code>{1,2,3,4}</code> a <code>{3,4,5,6}</code> vráti <code>{3, 4}</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Výsledok <code>setA.subtract(setB)</code> je rovnaký ako <code>setB.subtract(setA)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Rozdiel množín nie je komutatívny - <code>setA.subtract(setB)</code> vráti prvky iba zo <code>setA</code>, ktoré nie sú v <code>setB</code>, a naopak.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Do <code>MutableSet</code> môžeme pridať prvok aj pomocou operátora <code>+=</code>, napríklad <code>set3 += 5</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Rovnako ako pri mapách, aj sety podporujú skrátený zápis pridávania cez operátor <code>+=</code>.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 14</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
