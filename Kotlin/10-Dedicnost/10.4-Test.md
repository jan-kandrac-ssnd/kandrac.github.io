# 10.4 Test - Dedičnosť (10.1 - 10.3)

Over si svoje vedomosti z kapitol o dedičnosti - kľúčové slová <code>open</code>, <code>override</code> a <code>super</code>. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 16</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Všetky triedy v Kotline sú implicitne <code>final</code>, teda z nich bežne nemôžeme dediť.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak chceme z triedy dediť, musíme ju explicitne označiť kľúčovým slovom <code>open</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Zápis <code>class Dog : Animal()</code> bez toho, aby bola trieda <code>Animal</code> označená <code>open</code>, sa skompiluje bez problémov.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Bez <code>open</code> je trieda <code>final</code> a pokus o dedenie z nej spôsobí chybu kompilácie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kotlin má triedy predvolene <code>final</code> z bezpečnostných dôvodov - na rozdiel od Javy, kde sú triedy implicitne <code>open</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Musíme sa explicitne rozhodnúť, že chceme umožniť dedičnosť, pomocou kľúčového slova <code>open</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>override</code> použijeme, keď chceme v podtriede zmeniť správanie funkcie z rodičovskej triedy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Funkciu môžeme prepísať len vtedy, ak je v rodičovskej triede zároveň označená ako <code>open</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Ak je funkcia v rodičovskej triede <code>open</code>, môžeme ju v podtriede prepísať aj bez použitia kľúčového slova <code>override</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>override</code> je v Kotline povinné - bez neho funkciu prepísať nemôžeme, aj keby bola v rodičovi <code>open</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Rovnako ako funkcie, môžeme v Kotline prepisovať aj atribúty pomocou <code>open val</code> a <code>override val</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>open val color: String = "Biela"</code> v rodičovi a <code>override val color: String = "Cervena"</code> v podtriede.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Kľúčové slovo <code>super</code> použijeme, keď chceme z podtriedy zavolať pôvodnú implementáciu funkcie z rodičovskej triedy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>super.speak()</code> v prepísanej metóde zavolá pôvodnú implementáciu z rodičovskej triedy pred vykonaním vlastného kódu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>super</code> môžeme použiť aj na zavolanie konštruktora rodičovskej triedy, napríklad <code>class Dog(name: String) : Animal(name)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Volanie konštruktora rodičovskej triedy je súčasťou dedičnosti - zápis <code>: Animal(name)</code> zavolá konštruktor rodiča priamo.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Ak trieda implementuje dve rozhrania s rovnakou predvolenou metódou, Kotlin si automaticky vyberie implementáciu podľa poradia dedičnosti.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> V takom prípade musíme metódu prepísať a explicitne zvoliť, ktorú implementáciu chceme zavolať, pomocou zápisu <code>super&lt;A&gt;.greet()</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Zápis <code>super&lt;A&gt;.greet()</code> slúži na explicitné určenie, z ktorého rozhrania sa má predvolená metóda zavolať.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez zátvoriek <code>&lt;A&gt;</code> by kompilátor nevedel, ktorú z viacerých implementácií má zavolať.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Trieda <code>Dog : Animal()</code> dedí od <code>Animal</code> všetky jej verejné (napr. <code>public</code>) funkcie a atribúty.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Podtrieda automaticky získava prístup k verejným funkciám a atribútom rodičovskej triedy.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcia z rodičovskej triedy, ktorá nie je označená <code>open</code>, sa dá v podtriede prepísať, ak použijeme kľúčové slovo <code>override</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Bez <code>open</code> na strane rodiča kompilátor prepísanie zamietne bez ohľadu na <code>override</code> v podtriede.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Zoznam objektov rôznych podtried môžeme uložiť do premennej typu rodičovskej triedy a zavolať na nich prepísanú metódu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Toto je typický príklad polymorfizmu - pri volaní sa vždy použije konkrétna (prepísaná) implementácia danej podtriedy.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Konštruktor rodičovskej triedy sa zavolá automaticky aj bez toho, aby sme ho explicitne uviedli v hlavičke podtriedy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Pri dedení z triedy (nie rozhrania) musíme konštruktor rodiča vždy explicitne zavolať, napr. <code>: Animal(name)</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Rozhranie môže mať predvolenú (default) implementáciu funkcie, ktorú trieda dedičiaca viacero rozhraní môže potrebovať vyriešiť pomocou <code>super&lt;...&gt;</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ide presne o prípad, keď dve rozhrania definujú rovnakú predvolenú metódu a je nutné explicitne vybrať, ktorú zavolať.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Volanie <code>super.speak()</code> na začiatku prepísanej funkcie umožňuje kombinovať pôvodné správanie rodiča s novým správaním podtriedy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Takto nemusíme duplikovať kód rodiča - jednoducho ho zavoláme a pridáme vlastnú logiku.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 16</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
