# 11.2 Test - Abstraktné triedy (11.1)

Over si svoje vedomosti z kapitoly o abstraktných triedach. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 11</p>

<div class="quiz-item active" data-correct="no">
  <p class="quiz-question">Abstraktnú triedu (<code>abstract class</code>) môžeme priamo instanciovať pomocou <code>Shape()</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Abstraktná trieda nemôže byť priamo instanciovaná - slúži len ako základ pre iné (konkrétne) triedy.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Abstraktná funkcia (napr. <code>abstract fun area(): Double</code>) nemá telo a musí byť implementovaná v podtriede.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Každá konkrétna podtrieda musí pomocou <code>override</code> doplniť telo pre všetky abstraktné funkcie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Abstraktná trieda môže popri abstraktných funkciách obsahovať aj bežné (implementované) funkcie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad funkcia <code>describe()</code> môže mať plnú implementáciu a interne volať abstraktné funkcie ako <code>area()</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Na rozdiel od rozhrania, abstraktná trieda môže mať vlastný konštruktor.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>abstract class Vehicle(val speed: Int)</code> má konštruktor s atribútom <code>speed</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Trieda môže dediť z viacerých abstraktných tried naraz, podobne ako implementovať viacero rozhraní.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> V Kotline môžeme dediť len z jednej triedy (aj abstraktnej), ale implementovať viacero rozhraní.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Abstraktnú triedu použijeme vtedy, keď chceme medzi podtriedami zdieľať spoločný kód (atribúty alebo implementácie funkcií).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Rozhranie je vhodnejšie vtedy, keď potrebujeme len definovať spoločné správanie bez zdieľaného stavu.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Abstraktná trieda musí obsahovať aspoň jednu neabstraktnú (implementovanú) funkciu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Abstraktná trieda môže obsahovať výhradne abstraktné funkcie - žiadna neimplementovaná funkcia nie je povinná.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Konkrétna (neabstraktná) podtrieda musí implementovať všetky abstraktné funkcie svojej rodičovskej abstraktnej triedy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak by niektorú abstraktnú funkciu neimplementovala, musela by byť sama tiež označená ako <code>abstract</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Triedy <code>Rectangle</code> a <code>Circle</code> dediace z abstraktnej triedy <code>Shape</code> môžu mať odlišnú implementáciu funkcie <code>area()</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Práve v tom je sila abstraktných tried - každá podtrieda si implementuje abstraktné funkcie po svojom.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Rozhranie je vhodnejšie ako abstraktná trieda vtedy, keď potrebujeme, aby všetky podtriedy zdieľali rovnaký konštruktor s atribútmi.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Rozhranie nemôže mať konštruktor ani atribúty s vlastným stavom - na to je určená práve abstraktná trieda.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia definovaná v abstraktnej triede bez kľúčového slova <code>abstract</code> musí mať vlastné telo (implementáciu).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Len funkcie explicitne označené <code>abstract</code> môžu zostať bez implementácie.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 11</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
