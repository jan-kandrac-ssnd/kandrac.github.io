# 14.3 Test - Build systémy (14.1 - 14.2)

Over si svoje vedomosti z kapitol o Gradle a knižniciach. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 12</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Gradle je build systém, ktorý sa používa vo väčšine Kotlin a Android projektov.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Umožňuje spravovať závislosti, kompiláciu a spúšťanie projektu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Súbor <code>build.gradle.kts</code> je zapísaný priamo v jazyku Kotlin (Kotlin DSL).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Prípona <code>.kts</code> označuje Kotlin skript - konfigurácia sa teda píše v samotnom Kotline.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Blok <code>repositories</code> v <code>build.gradle.kts</code> slúži na definovanie závislostí (knižníc), ktoré projekt potrebuje.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>repositories</code> definuje odkiaľ sťahujeme knižnice (napr. <code>mavenCentral()</code>). Samotné závislosti sa definujú v bloku <code>dependencies</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Skript <code>./gradlew run</code> spustí aplikáciu bez toho, aby sme museli mať Gradle nainštalovaný globálne v systéme.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>gradlew</code> (Gradle Wrapper) sa postará o stiahnutie správnej verzie Gradle automaticky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Blok <code>plugins</code> v <code>build.gradle.kts</code> určuje, aké pluginy (napr. <code>kotlin("jvm")</code>) projekt používa.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad plugin <code>application</code> umožňuje projekt priamo spúšťať cez <code>./gradlew run</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Do bloku <code>dependencies</code> pridávame knižnice pomocou kľúčového slova <code>plugins</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Knižnice pridávame pomocou <code>implementation("skupina:artefakt:verzia")</code>, napríklad <code>implementation("com.google.code.gson:gson:2.11.0")</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Po pridaní novej závislosti do <code>build.gradle.kts</code> je potrebné nechať IntelliJ Idea načítať zmeny Gradle (napr. cez tlačidlo "Load Gradle Changes").</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez synchronizácie by IDE nevedelo o novo pridanej knižnici a jej triedy by neboli dostupné.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Knižnica Gson slúži na serializáciu a deserializáciu objektov do a z formátu JSON.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>gson.toJson(person)</code> prevedie objekt na JSON reťazec a <code>gson.fromJson(json, Person::class.java)</code> ho spätne prevedie na objekt.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcia <code>gson.fromJson(json, Person::class.java)</code> vráti JSON reťazec z objektu triedy <code>Person</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>fromJson</code> robí opak - prevedie JSON reťazec na objekt daného typu. Opačným smerom (objekt na JSON) slúži <code>gson.toJson(...)</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>kotlinx-coroutines</code> je knižnica určená na asynchrónne programovanie v Kotline.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Podobne <code>kotlinx-serialization</code> je alternatíva ku Gson pre serializáciu a <code>okhttp</code> slúži ako HTTP klient.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Verziu knižnice v <code>build.gradle.kts</code> nie je nikdy potrebné aktualizovať - Gradle si automaticky vždy stiahne najnovšiu verziu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Verzia je súčasťou reťazca závislosti (napr. <code>2.11.0</code>) a treba ju manuálne aktualizovať - najnovšiu verziu si vieme overiť na Maven Central.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Adresár <code>src/main/kotlin/</code> je predvolené miesto, kam IntelliJ umiestni zdrojové súbory nového Gradle projektu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Táto štruktúra vzniká automaticky spolu so súbormi <code>build.gradle.kts</code> a <code>settings.gradle.kts</code>.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 12</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
