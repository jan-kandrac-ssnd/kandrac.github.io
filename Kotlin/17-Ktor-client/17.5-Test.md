# 17.5 Test - Ktor client (17.1 - 17.4)

Over si svoje vedomosti z kapitol o Ktor kliente - HTTP požiadavky, parsovanie odpovedí, výnimky a sealed triedy. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 17</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Ktor je framework, ktorý sa dá použiť ako HTTP klient aj ako HTTP server.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> V kapitole 17 sa venujeme klientovi, v kapitole 18 potom serverovej časti.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>runBlocking</code> spustí coroutine a zablokuje hlavné vlákno, kým sa nedokončí.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Používa sa napríklad na to, aby sme mohli v <code>main()</code> volať <code>suspend</code> funkcie Ktor klienta.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Plugin <code>ContentNegotiation</code> s funkciou <code>json()</code> zabezpečuje automatické (de)serializovanie JSON tela požiadaviek a odpovedí.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Vďaka nemu vieme priamo volať <code>response.body&lt;User&gt;()</code> bez ručného parsovania JSON reťazca.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcia <code>client.post(...)</code> odošle GET požiadavku na daný endpoint.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>client.post(...)</code> odošle POST požiadavku. Na GET požiadavku slúži <code>client.get(...)</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Pri POST požiadavke telo požiadavky nastavíme pomocou funkcie <code>setBody(...)</code> vo vnútri bloku požiadavky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Spolu s <code>contentType(ContentType.Application.Json)</code> zabezpečí odoslanie objektu ako JSON telo požiadavky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>response.body&lt;T&gt;()</code> automaticky deserializuje telo odpovede do inštancie zadaného typu <code>T</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Funguje to aj pre kolekcie, napríklad <code>response.body&lt;List&lt;Todo&gt;&gt;()</code> vráti zoznam objektov.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Pomocou <code>parameter("userId", 1)</code> vieme do GET požiadavky pridať query parameter.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Výsledná URL by obsahovala <code>?userId=1</code> ako query string.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>response.status</code> vždy vráti hodnotu <code>200 OK</code>, nezávisle od skutočného výsledku požiadavky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>response.status</code> vracia skutočný HTTP stavový kód odpovede servera - preto by sme ho mali overiť pred spracovaním dát.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>ClientRequestException</code> reprezentuje chyby typu 4xx (napr. 404 Not Found).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Naopak <code>ServerResponseException</code> reprezentuje chyby typu 5xx a <code>RedirectResponseException</code> presmerovania 3xx.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>ServerResponseException</code> sa vyhodí pri chybách na strane klienta, napríklad pri neplatných vstupných dátach.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>ServerResponseException</code> reprezentuje chyby na strane servera (5xx). Chyby klienta (4xx) rieši <code>ClientRequestException</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Plugin <code>HttpTimeout</code> nám umožňuje nastaviť časový limit (napr. <code>requestTimeoutMillis</code>) pre HTTP požiadavky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak požiadavka trvá dlhšie ako nastavený limit, klient ju preruší výnimkou.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Plugin <code>HttpRequestRetry</code> umožňuje automaticky zopakovať požiadavku pri chybe servera.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>maxRetries = 3</code> spolu s <code>retryOnServerErrors(3)</code> zopakuje požiadavku pri zlyhaní na strane servera.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Sealed triedu, napr. <code>NetworkResult</code> so stavmi <code>Loading</code>, <code>Success</code> a <code>Error</code>, je vhodné použiť na reprezentáciu výsledku sieťovej požiadavky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Tento vzor (sealed class + when) je bežný pri spracovaní stavov UI, napríklad v Android vývoji.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcia označená kľúčovým slovom <code>suspend</code> môžeme zavolať z bežnej (nesuspendovanej) funkcie bez akéhokoľvek obmedzenia.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>suspend</code> funkciu môžeme zavolať len z inej <code>suspend</code> funkcie alebo z coroutine (napr. cez <code>runBlocking</code>).</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Pri spracovaní chýb v <code>fetchTodos</code> je vhodné obaliť sieťové volanie do <code>try-catch</code> a pri zlyhaní vrátiť napríklad <code>NetworkResult.Error</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Takto premeníme výnimku na explicitný stav, ktorý vieme ďalej spracovať pomocou <code>when</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Klienta (<code>HttpClient</code>) by sme mali po skončení práce s ním zatvoriť volaním <code>client.close()</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Uvoľní sa tým sieťové spojenie a ostatné zdroje, ktoré klient používal.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Kontrola <code>if (response.status == HttpStatusCode.OK)</code> je zbytočná, keďže Ktor automaticky vyhodí výnimku pri akomkoľvek stavovom kóde inom ako 200.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Takáto kontrola je užitočná najmä vtedy, keď chceme rozlíšiť úspech od zlyhania bez toho, aby sme sa spoliehali výhradne na výnimky.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 17</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
