# 18.5 Test - Ktor server (18.1 - 18.4)

Over si svoje vedomosti z kapitol o Ktor serveri - setup, GET/POST požiadavky a napojenie na databázu. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 17</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Funkcia <code>embeddedServer(Netty, port = 8080) { ... }</code> vytvorí a spustí Ktor server na porte 8080.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>Netty</code> je tu engine, ktorý server pod kapotou používa.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Volanie <code>.start(wait = true)</code> na serveri spôsobí, že program počká (beží), kým server neukončíme.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez <code>wait = true</code> by <code>main()</code> mohol skončiť ešte skôr, než server stihne obslúžiť požiadavky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Blok <code>routing { get("/") { ... } }</code> definuje, ako má server odpovedať na GET požiadavku na koreňovú cestu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Vo vnútri bloku máme prístup k <code>call</code>, cez ktorý napríklad odpovieme textom pomocou <code>call.respondText(...)</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcia <code>call.respond(dáta)</code> vždy vráti dáta ako obyčajný text, aj keď je nainštalovaný <code>ContentNegotiation</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Vďaka nainštalovanému <code>ContentNegotiation</code> s <code>json()</code> sa objekt automaticky serializuje do JSON.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Cestový parameter definovaný ako <code>{id}</code> v <code>get("/users/{id}")</code> vieme v handleri prečítať pomocou <code>call.parameters["id"]</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Vrátená hodnota je typu <code>String?</code>, preto ju často prevádzame napríklad pomocou <code>toIntOrNull()</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>call.receive&lt;T&gt;()</code> spracuje telo POST požiadavky a deserializuje ho do typu <code>T</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>call.receive&lt;User&gt;()</code> prevedie prijaté JSON telo na inštanciu triedy <code>User</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Po úspešnom vytvorení zdroja by mal server odpovedať stavovým kódom <code>HttpStatusCode.Created</code> (201).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>call.respond(HttpStatusCode.Created, savedUser)</code> vráti aj stavový kód, aj samotný vytvorený objekt.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Ktor podporuje len metódy GET a POST - metódy PUT a DELETE nie sú v routingu dostupné.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Ktor podporuje aj <code>put(...)</code> a <code>delete(...)</code> v rámci bloku <code>routing</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Príkaz <code>curl -X POST -H "Content-Type: application/json" -d '{"name":"Novy"}' http://localhost:8080/users</code> odošle POST požiadavku s JSON telom.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Parameter <code>-X POST</code> nastavuje HTTP metódu, <code>-H</code> hlavičku a <code>-d</code> telo požiadavky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Knižnica Exposed slúži ako DSL (doménovo-špecifický jazyk) pre prácu s databázou v Kotline.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Umožňuje definovať tabuľky a vykonávať SQL operácie priamo pomocou Kotlin kódu.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">H2 je databáza určená výhradne pre veľké produkčné nasadenia, nikdy sa nepoužíva na ukážky alebo testovanie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> H2 je naopak ľahká embedded databáza vhodná najmä na ukážky a testovanie. V reálnom produkčnom projekte by sme skôr použili PostgreSQL alebo MySQL.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Databázové operácie v Exposed sa vykonávajú vo vnútri bloku <code>transaction { ... }</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>Users.selectAll()</code> alebo <code>Users.insertAndGetId { ... }</code> musia byť volané vo vnútri transakcie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Tabuľka definovaná ako <code>object Users : Table()</code> musí mať definovaný primárny kľúč, napríklad <code>override val primaryKey = PrimaryKey(id)</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez primárneho kľúča by tabuľka nemala jednoznačný identifikátor pre jednotlivé záznamy.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkciu <code>SchemaUtils.create(Users)</code> je potrebné volať pri každom spustení servera opakovane vo vnútri každého endpointu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Táto funkcia sa typicky volá raz pri inicializácii databázy (napr. v <code>initDB()</code>), nie opakovane v každom endpointe.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Odporúčaná štruktúra väčšieho Ktor projektu zvyčajne oddeľuje <code>routes</code>, <code>models</code> a <code>plugins</code> do samostatných súborov.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Toto rozdelenie zlepšuje prehľadnosť a udržateľnosť projektu v porovnaní s jedným veľkým súborom <code>Main.kt</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Server môže bežať aj na inom porte ako 8080, ak parameter <code>port</code> zmeníme napríklad na <code>8081</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Port <code>8080</code> je len predvolená hodnota v ukážkach, nie pevne daná vlastnosť Ktor servera.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Funkcie na vloženie a načítanie dát z databázy musia byť definované priamo vo vnútri <code>routing</code> bloku a nedajú sa z neho vyňať do samostatných funkcií.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Naopak je vhodné (a bežné) mať samostatné funkcie ako <code>getAllUsers()</code> alebo <code>addUser(...)</code>, ktoré endpointy len volajú.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 17</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
