# 20.4 Test - JetPack Compose Texty a Tlačidlá (20.1 - 20.3)

Over si svoje vedomosti z kapitol o textoch, funkciách vyššieho rádu a tlačidlách v Compose. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 15</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question">Funkcia <code>Text("...")</code> zobrazí text a jej vzhľad vieme upraviť parametrami ako <code>fontSize</code>, <code>color</code> alebo <code>fontWeight</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>Text("Velky text", fontSize = 24.sp)</code> zväčší veľkosť písma.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>style = TextStyle(...)</code> umožňuje nastaviť viacero textových vlastností naraz na jednom mieste.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>TextStyle(fontSize = 20.sp, fontWeight = FontWeight.Medium, color = Color.Blue)</code> zoskupí viacero štýlov do jedného objektu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>Modifier</code> je reťazec modifikátorov, ktoré menia vzhľad alebo správanie composable prvku.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>Modifier.padding(16.dp)</code> pridá prvku vnútorný odstup.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>remember</code> uchová hodnotu premennej trvalo, aj po reštarte aplikácie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>remember</code> si pamätá hodnotu len počas životnosti danej composable funkcie (napr. medzi rekompozíciami), nie trvalo medzi spusteniami aplikácie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcie vyššieho rádu sú funkcie, ktoré prijímajú alebo vracajú iné funkcie ako parameter/hodnotu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>fun performAction(times: Int, action: () -> Unit)</code> prijíma funkciu <code>action</code> ako parameter.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Lambda výraz je skrátený zápis anonymnej funkcie bez mena.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>val add = { a: Int, b: Int -> a + b }</code> je lambda uložená v premennej <code>add</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Ak je lambda posledným parametrom funkcie, môžeme ju zapísať mimo okrúhlych zátvoriek, napríklad <code>doSomething("Test") { println("Hello") }</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Toto je bežná konvencia v Kotline (tzv. trailing lambda), veľmi často použitá aj pri composable funkciách.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>onClick</code> composable funkcie <code>Button</code> je príkladom funkcie vyššieho rádu (callback).</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>onClick: () -> Unit</code> je funkcia, ktorá sa zavolá po kliknutí na tlačidlo.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>Button</code>, <code>OutlinedButton</code> a <code>TextButton</code> majú vždy identický vizuálny vzhľad, líšia sa len názvom.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>Button</code> má plné pozadie, <code>OutlinedButton</code> len obrys a <code>TextButton</code> nemá pozadie ani obrys - vizuálne sa teda výrazne líšia.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>enabled = count < 10</code> na tlačidle deaktivuje tlačidlo, keď je táto podmienka nepravdivá.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak je <code>count &gt;= 10</code>, výraz <code>count < 10</code> vráti <code>false</code> a tlačidlo sa stane neaktívnym.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>FloatingActionButton</code> je typ tlačidla, ktoré "pláva" nad ostatným obsahom obrazovky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Typicky sa používa na hlavnú (najčastejšiu) akciu na danej obrazovke.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Do tlačidla <code>Button</code> môžeme vložiť len jeden text - nemôžeme doňho pridať aj ikonu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Do tlačidla môžeme vložiť aj <code>Icon(...)</code> spolu s <code>Text(...)</code>, keďže obsah tlačidla je bežný composable blok.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Funkcia <code>repeat(n) { i -> println(i) }</code> využíva lambdu so skrátenou formou zápisu jediného parametra.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak má lambda jediný parameter, môžeme ho pomenovať priamo v hlavičke (<code>i -></code>) alebo použiť implicitné <code>it</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">V Compose sú callbacky ako <code>onValueChange</code> pri <code>TextField</code> takmer vždy zapísané ako lambda výrazy.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>onValueChange = { newText -> text = newText }</code> je typický zápis reakcie na zmenu textu.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Ak chceme počítadlo, ktoré sa zvyšuje po kliknutí na tlačidlo, musíme jeho hodnotu ukladať do premennej mimo composable funkcie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Stav vytvárame priamo vo vnútri composable funkcie pomocou <code>remember { mutableStateOf(0) }</code>, nemusíme ho ukladať mimo nej.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 15</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
