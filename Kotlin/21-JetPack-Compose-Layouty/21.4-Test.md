# 21.4 Test - JetPack Compose Layouty (21.1 - 21.3)

Over si svoje vedomosti z kapitol o layoutoch Column, Row a Box v Compose. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 14</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question"><code>Column</code> zoraďuje svoje prvky pod seba, teda vertikálne.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>Column</code> je ekvivalent <code>flex-direction: column</code> v CSS.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>Row</code> zoraďuje svoje prvky pod seba, rovnako ako <code>Column</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>Row</code> naopak zoraďuje prvky vedľa seba (horizontálne), na rozdiel od <code>Column</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>verticalArrangement</code> v <code>Column</code> určuje rozloženie prvkov vertikálne, napríklad medzery medzi nimi.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>Arrangement.spacedBy(8.dp)</code> pridá medzi prvky pravidelnú medzeru 8 dp.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Predvolená hodnota <code>horizontalAlignment</code> v <code>Column</code> je <code>Alignment.CenterHorizontally</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Predvolená hodnota je <code>Alignment.Start</code> - prvky sú zarovnané doľava, pokiaľ explicitne nenastavíme inak.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">V <code>Row</code> parameter <code>verticalAlignment = Alignment.CenterVertically</code> zarovná prvky na stred vertikálne.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Predvolená hodnota je <code>Alignment.Top</code>, ale túto vieme zmeniť napríklad na centrovanie.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>horizontalArrangement = Arrangement.SpaceBetween</code> v <code>Row</code> rozloží prvky tak, že medzi nimi je rovnomerný priestor, no na krajoch nie.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Prvý prvok sa dotýka ľavého okraja a posledný pravého - priestor sa rozloží iba medzi ne.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>Box</code> umožňuje prekrývať prvky cez seba, podobne ako <code>position: relative</code> v CSS.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Prvky v <code>Box</code> sa kladú na seba v poradí, v akom sú definované.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>Box</code> je najvhodnejší layout na klasické, neprekrývajúce sa rozloženie viacerých prvkov v rade alebo v stĺpci.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Na klasické rozloženie slúžia <code>Column</code> a <code>Row</code>. <code>Box</code> je vhodný najmä na prekrývanie prvkov.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>contentAlignment</code> v <code>Box</code> určuje, ako sú vnorené prvky zarovnané v rámci celého boxu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>contentAlignment = Alignment.Center</code> vycentruje všetky vnorené prvky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>Box</code> je vhodný na vytvorenie napríklad malého číselného odznaku (badge) v rohu ikony.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Prekrytie ikony malým textovým prvkom v rohu je jeden z typických prípadov použitia <code>Box</code>.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>Arrangement</code> a <code>Alignment</code> znamenajú v Compose to isté a dajú sa navzájom zamieňať.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>Arrangement</code> určuje rozloženie (medzery, distribúciu priestoru) v smere hlavnej osi, zatiaľ čo <code>Alignment</code> zarovnanie v priečnom smere.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>Modifier.padding(16.dp)</code> môžeme použiť na <code>Column</code>, <code>Row</code> aj <code>Box</code> rovnako ako na iné composable prvky.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>Modifier</code> je univerzálny mechanizmus použiteľný na všetky layouty aj bežné prvky.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Vnorený <code>Box(modifier = Modifier.size(150.dp).background(Color.Cyan))</code> vo vnútri väčšieho <code>Box</code>-u vytvorí prekrývajúcu sa farebnú vrstvu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Viacero vnorených <code>Box</code> prvkov s rôznou veľkosťou a farbou pozadia sa jednoducho prekryje podľa poradia deklarácie.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>Row</code> a <code>Column</code> nemôžu byť vnorené jeden do druhého - Compose layouty sa nedajú kombinovať.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Layouty sa bežne kombinujú a vnárajú - napríklad <code>Row</code> vnútri <code>Column</code> je veľmi bežný vzor pri tvorbe formulárov.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 14</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
