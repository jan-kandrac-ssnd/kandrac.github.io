# 22.4 Test - JetPack Compose vstupné textové polia (22.1 - 22.3)

Over si svoje vedomosti z kapitol o textových poliach TextField, OutlinedTextField a BasicTextField v Compose. Klikni na **Áno** alebo **Nie** - tlačidlo sa hneď zafarbí (zelená = správne, červená = nesprávne) a zobrazí sa komentár s vysvetlením. Otázky prechádzaš po jednej pomocou tlačidiel nižšie.

<div id="quiz-container">

<div id="quiz-questions">

<p id="quiz-progress">Otázka 1 / 14</p>

<div class="quiz-item active" data-correct="yes">
  <p class="quiz-question"><code>TextField</code> je základné textové pole z Compose Material 3, ktoré umožňuje používateľovi zadať text.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>TextField</code> je štandardná Material 3 komponenta na textový vstup.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Parameter <code>onValueChange</code> v <code>TextField</code> je voliteľný a dá sa vynechať.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>value</code> aj <code>onValueChange</code> sú povinné parametre - bez callbacku by pole nedokázalo reagovať na zmenu textu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Aktuálnu hodnotu textového poľa typicky uchovávame v premennej vytvorenej pomocou <code>remember { mutableStateOf("") }</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Bez uchovania stavu by sa napísaný text po rekompozícii stratil.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>label</code> a <code>placeholder</code> v <code>TextField</code> znamenajú úplne to isté a zobrazujú sa v rovnakom čase na rovnakom mieste.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>label</code> je popis nad/vo vnútri poľa, ktorý sa po napísaní textu zmenší, zatiaľ čo <code>placeholder</code> sa zobrazuje iba keď je pole prázdne.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Parameter <code>singleLine</code> určuje, či sa textové pole obmedzí na jeden riadok.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Ak nastavíme <code>singleLine = true</code>, pole nepovolí zalomenie textu na nový riadok.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>leadingIcon</code> a <code>trailingIcon</code> umožňujú zobraziť ikonu pred, respektíve za textom v poli.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Sú to voliteľné parametre, ktoré prijímajú composable obsah (napríklad <code>Icon</code>).</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>OutlinedTextField</code> má úplne iné parametre ako <code>TextField</code>, preto sa medzi nimi nedá jednoducho prepínať.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Oba typy majú rovnaké parametre (<code>value</code>, <code>onValueChange</code>, <code>label</code>...) - líšia sa iba vo vizuálnom štýle.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>OutlinedTextField</code> má viditeľný obrys namiesto vyplneného sivého pozadia ako <code>TextField</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Vizuálne je <code>OutlinedTextField</code> "čistejší" vďaka obrysu namiesto pozadia.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>OutlinedTextField</code> sa odporúča výhradne pre hlavné formuláre, zatiaľ čo <code>TextField</code> len pre vedľajšie polia.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> Je to naopak - <code>TextField</code> sa bežne používa na hlavné formuláre, <code>OutlinedTextField</code> skôr na vedľajšie polia. Výber je však najmä otázkou vizuálneho štýlu.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>BasicTextField</code> nemá žiadne predvolené štýlovanie ani label.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> <code>BasicTextField</code> je "holý" základ bez pozadia, obrysu či labelu - vzhľad si musíme dodať sami.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question"><code>BasicTextField</code> je vhodnou voľbou, keď potrebujeme plnú kontrolu nad vlastným vzhľadom poľa.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Je to základ, z ktorého sú postavené <code>TextField</code> aj <code>OutlinedTextField</code>, takže sa hodí na úplne vlastný dizajn.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question"><code>BasicTextField</code> automaticky pridáva pozadie a orámovanie rovnako ako <code>TextField</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>BasicTextField</code> nemá žiadne vizuálne štýlovanie - ak chceme pozadie alebo orámovanie, musíme ho obaliť napríklad do <code>Box</code> s vlastným <code>Modifier</code>.</p>
</div>

<div class="quiz-item" data-correct="yes">
  <p class="quiz-question">Vlastný vzhľad textu v <code>BasicTextField</code> vieme nastaviť pomocou parametra <code>textStyle</code> typu <code>TextStyle</code>.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Áno.</strong> Napríklad <code>TextStyle(fontSize = 18.sp, color = Color.Black)</code> nastaví veľkosť a farbu textu.</p>
</div>

<div class="quiz-item" data-correct="no">
  <p class="quiz-question">Parameter <code>enabled</code> pri textových poliach nemá žiaden efekt a slúži len na dokumentáciu kódu.</p>
  <div class="quiz-actions">
    <button type="button" class="quiz-btn quiz-yes">Áno</button>
    <button type="button" class="quiz-btn quiz-no">Nie</button>
  </div>
  <p class="quiz-comment"><strong>Nie.</strong> <code>enabled</code> reálne ovplyvňuje, či je pole aktívne - keď je <code>false</code>, používateľ doň nemôže písať.</p>
</div>

<div class="quiz-nav">
  <button type="button" id="quiz-prev" class="quiz-nav-btn" disabled>&larr; Predchádzajúca</button>
  <button type="button" id="quiz-next" class="quiz-nav-btn" disabled>Ďalšia &rarr;</button>
</div>

</div>

<div id="quiz-summary">
  <p class="quiz-summary-score">Tvoje skóre je <span id="quiz-score">0 / 14</span></p>
  <button type="button" id="quiz-restart" class="quiz-nav-btn">Absolvovať test znova</button>
</div>

</div>

<script>
  (function () {
    const questionsWrap = document.getElementById('quiz-questions');
    const summaryEl = document.getElementById('quiz-summary');
    const scoreEl = document.getElementById('quiz-score');
    const restartBtn = document.getElementById('quiz-restart');
    const items = Array.from(document.querySelectorAll('#quiz-container .quiz-item'));
    const progress = document.getElementById('quiz-progress');
    const prevBtn = document.getElementById('quiz-prev');
    const nextBtn = document.getElementById('quiz-next');
    let current = 0;

    function updateNav() {
      const isLast = current === items.length - 1;
      prevBtn.disabled = current === 0;
      nextBtn.disabled = !items[current].classList.contains('answered');
      nextBtn.textContent = isLast ? 'Vyhodnotiť →' : 'Ďalšia →';
    }

    function showItem(index) {
      items.forEach((item, i) => item.classList.toggle('active', i === index));
      progress.textContent = 'Otázka ' + (index + 1) + ' / ' + items.length;
      updateNav();
    }

    function answerItem(item, btn, correct) {
      if (item.classList.contains('answered')) return;
      item.classList.add('answered');

      const chosen = btn.classList.contains('quiz-yes') ? 'yes' : 'no';
      const isCorrect = chosen === correct;
      item.dataset.result = isCorrect ? 'correct' : 'incorrect';
      btn.classList.add(isCorrect ? 'correct' : 'incorrect');

      if (!isCorrect) {
        const correctBtn = item.querySelector(correct === 'yes' ? '.quiz-yes' : '.quiz-no');
        correctBtn.classList.add('correct');
      }

      item.querySelectorAll('.quiz-btn').forEach((b) => { b.disabled = true; });
      item.querySelector('.quiz-comment').classList.add('visible');
      updateNav();
    }

    items.forEach((item) => {
      const correct = item.dataset.correct;
      item.querySelectorAll('.quiz-btn').forEach((btn) => {
        btn.addEventListener('click', () => answerItem(item, btn, correct));
      });
    });

    function showSummary() {
      const score = items.filter((item) => item.dataset.result === 'correct').length;
      scoreEl.textContent = score + ' / ' + items.length;
      questionsWrap.style.display = 'none';
      summaryEl.classList.add('visible');
    }

    function resetQuiz() {
      items.forEach((item) => {
        item.classList.remove('answered');
        delete item.dataset.result;
        item.querySelectorAll('.quiz-btn').forEach((b) => {
          b.disabled = false;
          b.classList.remove('correct', 'incorrect');
        });
        item.querySelector('.quiz-comment').classList.remove('visible');
      });
      summaryEl.classList.remove('visible');
      questionsWrap.style.display = '';
      current = 0;
      showItem(current);
    }

    prevBtn.addEventListener('click', () => {
      if (current > 0) {
        current -= 1;
        showItem(current);
      }
    });

    nextBtn.addEventListener('click', () => {
      if (current === items.length - 1) {
        showSummary();
        return;
      }
      current += 1;
      showItem(current);
    });

    restartBtn.addEventListener('click', resetQuiz);

    showItem(current);
  })();
</script>
